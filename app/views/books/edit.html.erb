<h3>Editing Book</h3>
<%= form_with model: @book do |f| %>
<h4>Title</h4>
<%= f.text_field :title %>
<h4>Boby</h4>
<%= f.text_area :body %>
<%= f.submit 'Update Book' %>
<span><%= link_to 'show', book_path(@books) %></span>
<span><%= link_to 'back', 'books#index' %></span>
<% end %>